# Backend API URL (include /api)
# For development, this is typically http://localhost:3001/api
# For production, set this to your HTTPS backend URL to avoid mixed content
VITE_API_BASE_URL=https://your-backend-domain.com/api
